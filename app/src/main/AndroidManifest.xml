<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gonzalo.proyectofinalandroid">

    <!-- Agrego permisos para utilizar GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".CoordenadasDeUsuario" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".activity_conexion" />
        <activity android:name=".activity_RegistrarUsuario" />
        <activity android:name=".activityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FalsoMain"
            android:label="@string/title_activity_falso_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".listaRecorridoActivity" />
        <activity android:name=".tarifaActivity" />
        <activity android:name=".activity_horario" />
        <activity android:name=".activity_editar_usuario"></activity>
    </application>

</manifest>